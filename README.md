BEGIN
    SELECT MILL_SPEED
    INTO P_LINE_SPEED
    FROM T_PERIODIC_VALUE_LOG
    ORDER BY DATE_TIME DESC
    FETCH FIRST 1 ROW ONLY;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        P_LINE_SPEED := NULL;  -- Or some default value
END;